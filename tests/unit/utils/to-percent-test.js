import toPercent from 'demp-app/utils/to-percent';
import { module, test } from 'qunit';

module('Unit | Utility | to percent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toPercent();
  assert.ok(result);
});
